1. 端到端
	+ 指的是：主机到主机之间的通信
	+ 网络层提供的服务就是主机到主机，也就是端到端的通信

2. 传输层
	+ 提供的是进程到进程的通信

3. 尽力而为
	+ 指的是：服务是尽力而为的，也就是说，不保证数据的可靠传输，只是尽最大努力传输数据

4. p2p
	+ 指的是：peer to peer，也就是点对点的通信

5. 网络层是在链路层提供的相邻两天提供的点到点的服务实现了主机到主机的服务

6. 物理层
	+ 把数字新号发送的时候变成物理信号